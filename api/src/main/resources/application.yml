spring:
  jpa:
    hibernate:
      ddl-auto: update # TODO: Change this. Investigate proper database migrations
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: "jdbc:postgresql://winhost/librarydb"
    username: postgres
    password: password
